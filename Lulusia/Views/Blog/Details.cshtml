@model BlogClientViewModel
@{
    ViewData["Title"] = "Blog Details";
}
<div class="page-container my-4">
    <h1 class="text-center mb-3">@Model.Subject</h1>
    <div class="htmlContent">
        @Html.Raw(Model.Content)
    </div>
</div>