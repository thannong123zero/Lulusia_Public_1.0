@model InforPageClientViewModel
@{
    ViewData["Title"] = "About Us Page";
}
@if(Model != null){
    <div class="page-container my-4">
    <h1 class="text-center font-weight-600 text-uppercase">@Model.Title</h1>
    <div>
            @Html.Raw(Model.Content)
    </div>
</div>
}